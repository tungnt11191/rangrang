<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>
        <!--
        District
        -->
        <record id="view_country_state_district_tree" model="ir.ui.view">
            <field name="name">res.country.state.district.tree</field>
            <field name="model">res.country.state.district</field>
            <field name="arch" type="xml">
                <tree string="District" editable="bottom">
                    <field name="name"/>
                    <field name="code"/>
                    <field name="province_id" options="{'no_create': True}"/>
                </tree>
            </field>
        </record>

        <record id="view_country_state_district_form" model="ir.ui.view">
            <field name="name">res.country.state.district.form</field>
            <field name="model">res.country.state.district</field>
            <field name="arch" type="xml">
                <form string="District">
                    <sheet>
                        <group>
                            <field name="name"/>
                            <field name="code"/>
                            <field name="province_id" options='{"no_open": True, "no_create": True}'/>
                        </group>
                    </sheet>
                </form>
            </field>
        </record>


        <record id="action_district" model="ir.actions.act_window">
            <field name="name">Districts</field>
            <field name="type">ir.actions.act_window</field>
            <field name="res_model">res.country.state.district</field>
            <field name="view_id" ref="view_country_state_district_tree"/>
            <field name="help">Display and manage the list of all districts that can be assigned to your partner records. You can create or delete countries to make sure the ones you are working on will be maintained.</field>
        </record>

        <menuitem id="menu_district_group"
                  action="action_district"
                  name="Districts" parent="contacts.menu_localisation"
                  sequence="2"/>
    </data>
</odoo>